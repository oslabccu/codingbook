#include <stdio.h>
1

int main(){
    /*
    char buf[128];
    FILE *pp;

    if((pp =popen("ls -l", "r")) == NULL)
    {
        printf("popen() error\n");
        exit(1);
    }

    while(fgets(buf, sizeof(buf), pp))
    {
        printf("%s",buf);
    }
    pclose(pp);

    printf("~\n");

    int a = system("ls -l");
    printf("%d\n", a);
    */


    return 0;
}
